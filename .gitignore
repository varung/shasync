# compiled binary at the repo root
/shasync

# editor / OS scratch
*.swp
.DS_Store
/tags

# personal working trees tracked by shasync itself
/notes

# test-time scratch (none expected, but in case)
/tmp_test_*
/myvcs
